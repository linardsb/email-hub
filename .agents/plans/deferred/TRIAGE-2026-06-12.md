# Deferred-stub triage — fork (a) keep/drop (2026-06-12, Phase 53 Track E)

> Recommendation record for the ×23 stub promotions. Fork (a) ratified at the 53.1 gate;
> the VLM verify→correct loop was RETIRED at 53.4 (`.agents/plans/53-4-vlm-retirement.md`).
> Each KEEP still gets its own detailed plan at promotion — this file only sorts the pile.
> User ratification pending; nothing here deletes a stub file.

## RETIRE — delivery mechanism was the VLM correction loop, which is retired (7)

| Stub | Why retired |
|---|---|
| `52.1-multi-viewport-rendering.md` | Extends `run_verification_loop()` — the retired loop. Multi-viewport *capture* could resurface inside A3 scoring if ever needed; that would be a new plan. |
| `52.2-categorized-correction-schema.md` | VLM correction output schema — no consumer. |
| `52.3-structural-correction-applicator.md` | Extends the retired `correction_applicator`. Its `wrap_in`/`add_class` mutators are NOT needed by 52.4/52.6/52.7 once those re-scope to direct renderer emission (below). |
| `52.8-rule-4-visible-divider-absent.md` | PNG-diff + VLM insertion path. The deterministic half (divider stroke fidelity) is covered by `.agents/plans/53-5-vector-recovery.md`. |
| `52.9-rule-5-asset-name-vs-content.md` | Framed on the VLM loop + perceptual hashing. If asset misclassification resurfaces as a real defect, re-scope deterministically (aspect ratio + sibling order already exist per 50.7) — new plan, not this stub. |
| `52.10-rule-telemetry.md` | Telemetry for corrections that no longer happen (depends on 52.2's rule_id schema). |
| `53.1-verification-reuse-repeating-groups.md` | Caches VLM corrections across repeating-group members — no loop, nothing to cache. Counts were closed deterministically (D1/D3). |

## CLOSE / FOLDED — already delivered under another name (2)

| Stub | Disposition |
|---|---|
| `52.5-rule-3-image-padding-asymmetric.md` | Capture shipped in 52.5 RC-E (`a6d2afa1`); per-side padding emission shipped in RC-D-prime (`8c5cffd7`). Residual (mobile `img-pad` class override) is a one-liner to fold into 52.4's responsive work if kept. |
| `53.3-per-frame-export-default.md` | Folded into `.agents/plans/53-3-ingest-render.md` §53.3c/d (crop-via-frame-export + reproducibility-classifier fallback). |

## KEEP — composite-slot family, strictly ordered behind 51.1 (6)

| Stub | Note |
|---|---|
| `51.1-composite-slot-infrastructure.md` | **The foundational blocker** — `SlotFill` discriminated union + sub-renderer recursion (depth ≤3). Nothing below lands without it. Promote FIRST. |
| `51.2-rule-1-card-with-n-children.md` | Re-scope at promotion: band grouping (D1) already ships the wrapper-level half; what remains is the rounded-card composite render for card-with-N-children. |
| `51.3-tag-pill-slot.md` | Tag/pill slot; Rule 7/8 predicates exist since 50.5. |
| `51.4-spec-mini-table-slot.md` | Spec-list composite (icon+label pairs). |
| `51.5-multi-line-text-splitter.md` | Utility for 51.4. NOTE at promotion: RC-D-prime per-node `<td>` anchors changed body emission — the `\n`→`<br>` fallback path cited at `_safe_text` may already suffice; verify before building. |
| `51.6-composite-footer-cleanup.md` | Routing refactor; depends 51.1 + 51.2. |

## KEEP — re-scope as direct matcher/renderer emission (no correction applicator) (3)

| Stub | Re-scope |
|---|---|
| `52.4-rule-2-responsive-image-overflow.md` | Emit `bannerimg` class at render time for images in column layouts + shell CSS. No 52.3 dependency once direct. |
| `52.6-rule-6-mobile-dom-order-rtl.md` | Image-right flip via `dir="rtl"` markup emitted by the column seed path directly. Proven pattern in `manual_component_build.html` §10/§12. |
| `52.7-rule-9-dark-mode-nested-cards.md` | `card-bg-N` dark-mode classes emitted with band/inner-card overrides (50.4 `inner_bg` + 50.7 physical-card exception both shipped). Pairs with the Rule-9 descendant-granularity debt noted on `phase-50.8-nested-physical-cards`. |

## KEEP — classifier/quality, engine-neutral (5)

| Stub | Note |
|---|---|
| `53.2-hero-text-image-pairing.md` | Boundary-color pair merge; 50.2 inputs shipped. |
| `53.4-transition-band-detection.md` | Geometry heuristic on shipped boundary data. |
| `53.5-golden-reference-confidence-boost.md` | Small scoring tweak; promote opportunistically. |
| `53.6-reference-set-extension.md` | New regression designs — valuable, but mind `phase-53.7-asset-reexport-prerequisite` (new fixtures need committed or re-exportable assets). Gate behind Rules 1-3 landing. |
| `53.7-quality-checklist-automated-gate.md` | Deterministic 14-check gate; overlaps golden-conformance — at promotion, diff against existing checks first to avoid a second half-overlapping gate. |

## Net

- 7 retire · 2 close/folded · 14 keep (6 composite-slot chain → 3 renderer re-scopes → 5 quality/classifier).
- Suggested promotion order: **51.1 → 51.3 → 51.2 → 51.4/51.5 → 51.6 → 52.7 → 52.4 → 52.6 → 53.2/53.4 → 53.5/53.6/53.7**.
